{
  "name": "md-checkbox",
  "displayName": "Markdown Checkbox Replacer",
  "description": "Simple Markdown Checkbox State Toggler",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "md-checkbox.cycleNext",
        "title": "Cycle Next Checkbox State",
        "category": "Markdown Checkbox"
      },
      {
        "command": "md-checkbox.cycle_previous",
        "title": "Cycle Previous Checkbox State",
        "category": "Markdown Checkbox"
      }
    ],
    "keybindings": [
      {
        "key": "alt+q",
        "command": "md-checkbox.cycleNext",
        "when": "editorTextFocus"
      },
      {
        "key": "alt+shift+q",
        "command": "md-checkbox.cyclePrev",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "title": "Checkbox Replacer",
      "properties": {
        "md-checkbox.stages": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "[ ]",
            "[x]"
          ],
          "description": "Stages of checkbox states to cycle through"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.100.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2"
  }
}